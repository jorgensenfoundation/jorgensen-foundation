// Central backend API base URL. Every page script reads window.JF_API
// instead of hard-coding the Railway host, so we only change it in one place.
window.JF_API = 'https://jorgensen-backend-production.up.railway.app';
